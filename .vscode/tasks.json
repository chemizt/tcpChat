{
    "tasks": [
        {
            "type": "shell",
            "label": "build project with release configuration",
            "command": "cmake .. && make release",
            "options": {
                "cwd": "${workspaceRoot}\\build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "type": "shell",
            "label": "build project with debug configuration",
            "command": "cmake .. && make debug",
            "options": {
                "cwd": "${workspaceRoot}\\build"
            }
        }
    ],
    "version": "2.0.0"
}